import { TopBack } from '../../assets/img/index';

const styles = {
  marginLeft: '10px',
  marginTop: '10px',
  marginBottom: '10px',
};

const TopBar = () => {
  return <TopBack style={styles} />;
};

export default TopBar;
